package org.example.clic.service;

import org.springframework.stereotype.Service;

@Service
public class MailService {
    // Elimina o comenta la lógica de envío real de correo
    public void sendSimpleMail(String to, String subject, String text) {
        // No hacer nada, desactivado
        // Si prefieres, puedes dejarlo vacío o lanzar una excepción controlada si se usa por error.
    }
}
